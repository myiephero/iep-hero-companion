Perfect — here’s the fool-proof final markdown you can copy straight into your repo/docs.
It includes: ✅ patch code, ✅ Info.plist changes, ✅ capacitor config update, ✅ test plan.

⸻

🚀 iOS Safari Redirect Fix — Fool-Proof Guide

Problem

Our iOS Capacitor app was redirecting to Safari during authentication (replit.dev/auth) instead of staying inside the WebView.

Root Cause
	•	Capacitor’s default config didn’t whitelist replit.dev domains.
	•	Our AppDelegate patches never ran because iOS 13+ uses SceneDelegate lifecycle.
	•	As a result, WKWebView wasn’t intercepting navigation → Safari opened external URLs.

⸻

✅ Final Fix

1. Update capacitor.config.ts

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myapp.mobile',
  appName: 'MyApp',
  webDir: 'dist',
  server: {
    allowNavigation: [
      '*.janeway.replit.dev',
      'afd4ab41-fa60-4e78-9742-69bb4e3004d6-00-6i79wn87wfhu.janeway.replit.dev',
    ],
  },
};

export default config;

Then run:

npx cap sync ios


⸻

2. Add SceneDelegate.swift

Create a new file:
ios/App/App/SceneDelegate.swift

import UIKit
import Capacitor
import WebKit

class SceneDelegate: UIResponder, UIWindowSceneDelegate {
    var window: UIWindow?

    func scene(_ scene: UIScene,
               willConnectTo session: UISceneSession,
               options connectionOptions: UIScene.ConnectionOptions) {

        guard let windowScene = (scene as? UIWindowScene) else { return }
        let window = UIWindow(windowScene: windowScene)
        self.window = window
        window.makeKeyAndVisible()

        if let vc = window.rootViewController as? CAPBridgeViewController {
            if let webView = vc.bridge?.webView {
                webView.navigationDelegate = self
                webView.uiDelegate = self
                print("[SUCCESS] WebView delegates attached ✅")
            }
        }
    }
}

extension SceneDelegate: WKNavigationDelegate, WKUIDelegate {
    func webView(_ webView: WKWebView,
                 decidePolicyFor navigationAction: WKNavigationAction,
                 decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {

        guard let url = navigationAction.request.url else {
            decisionHandler(.cancel)
            return
        }

        print("[WKWebView] Navigation attempt →", url.absoluteString)

        if url.host?.contains("replit.dev") == true ||
           url.host?.contains("yourdomain.com") == true {
            decisionHandler(.allow) // Stay inside WebView
        } else {
            decisionHandler(.cancel)
            print("[BLOCKED] Safari redirect prevented →", url.absoluteString)
        }
    }

    func webView(_ webView: WKWebView,
                 createWebViewWith configuration: WKWebViewConfiguration,
                 for navigationAction: WKNavigationAction,
                 windowFeatures: WKWindowFeatures) -> WKWebView? {
        print("[WKUIDelegate] Blocked popup")
        return nil
    }
}


⸻

3. Update Info.plist

Inside ios/App/App/Info.plist add:

<key>UIApplicationSceneManifest</key>
<dict>
  <key>UIApplicationSupportsMultipleScenes</key>
  <false/>
  <key>UISceneConfigurations</key>
  <dict>
    <key>UIWindowSceneSessionRoleApplication</key>
    <array>
      <dict>
        <key>UISceneConfigurationName</key>
        <string>Default Configuration</string>
        <key>UISceneDelegateClassName</key>
        <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
      </dict>
    </array>
  </dict>
</dict>


⸻

🧪 Test Plan

Step 1. Clean + Rebuild

npx cap sync ios
npx cap open ios

	•	Run in iOS Simulator or on device.

⸻

Step 2. Check Logs

Look in Xcode console for:

[SUCCESS] WebView delegates attached ✅
[WKWebView] Navigation attempt → https://afd4ab41-fa60...

	•	If you see these → delegate is attached correctly.
	•	If not → check that SceneDelegate.swift is listed in Info.plist.

⸻

Step 3. Authentication Flow
	1.	Launch app → login form loads in WebView.
	2.	Perform login.
	3.	Confirm redirect stays in-app instead of Safari.
	4.	Console should log:

[WKWebView] Navigation attempt → https://afd4ab41-fa60.../auth



⸻

Step 4. Negative Test
	•	Try opening an external link (e.g. https://google.com).
	•	Confirm it is blocked with console log:

[BLOCKED] Safari redirect prevented → https://google.com



⸻

✅ Expected Outcome
	•	Replit login flow now remains inside WebView.
	•	Safari never opens for auth.
	•	Delegates show logging in Xcode.
	•	External links are blocked by default.

⸻

Would you like me to also generate the ready-to-commit Git patch file (so you can just git apply it), or is this markdown guide enough?